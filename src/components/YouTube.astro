---
import 'lite-youtube-embed/css/lite-yt-embed.css';

interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---
import 'lite-youtube-embed';

<lite-youtube videoid={id} playlabel={title}></lite-youtube>
